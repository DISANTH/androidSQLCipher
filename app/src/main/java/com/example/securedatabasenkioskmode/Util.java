package com.example.securedatabasenkioskmode;

public class Util {
    public static String genRandom(char[] random) {
        String x = "";
        for (int i = 0; i < random[i]; i++)
            x += random[i];

        return x;
    }
}
